# Where the Laravel API lives. Left blank, the app calls /api/v1 on its own
# origin and Vite's dev proxy forwards to VITE_API_PROXY_TARGET.
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=http://hackathon-be.test
